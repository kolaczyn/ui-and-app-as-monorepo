{
  "private": true,
  "name": "yarn-workspaces",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "turbo": "^1.5.6"
  }
}
